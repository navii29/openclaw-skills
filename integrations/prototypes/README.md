# 🔄 Integration Prototypes Index

## Quick Start

All prototypes are in `/integrations/prototypes/` and can be run standalone.

### Prerequisites
```bash
pip install requests
```

### Environment Variables
```bash
# Shopify
export SHOPIFY_TOKEN="shpat_..."

# Stripe
export STRIPE_API_KEY="sk_test_..."

# WooCommerce
export WC_CONSUMER_KEY="ck_..."
export WC_CONSUMER_SECRET="cs_..."
```

---

## Prototypes

### 1. WooCommerce (`woocommerce_prototype.py`)
**Market:** 30%+ of German E-Commerce sites

```bash
# List recent orders
python woocommerce_prototype.py --url https://shop.de --action list_orders

# Get order formatted for sevDesk
python woocommerce_prototype.py --url https://shop.de --action get_order --order-id 123

# Generate VAT report
python woocommerce_prototype.py --url https://shop.de --action vat_report --year 2024 --month 2

# Check low stock
python woocommerce_prototype.py --url https://shop.de --action low_stock
```

**Key Features:**
- Order → sevDesk/DATEV invoice formatting
- VAT calculation by rate (7%, 19%)
- Low stock alerts
- Abandoned cart detection

---

### 2. Stripe (`stripe_prototype.py`)
**Market:** Universal payment processing

```bash
# List customers
python stripe_prototype.py --action list_customers

# Sync invoices for accounting
python stripe_prototype.py --action sync_invoices --days 30

# Monthly revenue report
python stripe_prototype.py --action monthly_report --year 2024 --month 2

# List subscriptions
python stripe_prototype.py --action list_subscriptions
```

**Key Features:**
- SEPA support for German market
- Invoice sync to accounting systems
- Monthly tax reporting
- Webhook signature verification

---

### 3. Shopify (`shopify_prototype.py`)
**Market:** Growing E-Commerce platform

```bash
# List orders
python shopify_prototype.py --shop navii-demo --action list_orders

# Get orders ready for invoicing
python shopify_prototype.py --shop navii-demo --action invoice_ready --days 7

# Check inventory
python shopify_prototype.py --shop navii-demo --action get_inventory
```

**Key Features:**
- Rate limiting compliance
- Multi-location inventory
- Order → sevDesk format
- Webhook management

---

## API Research Docs

Located in `/integrations/api-research/`:

- `shopify-api.md` - Shopify REST API reference
- `stripe-api.md` - Stripe API with German market specifics

---

## Strategy Document

See `INTEGRATION_STRATEGY.md` for:
- Priority matrix
- Use case analysis
- Go-to-market plan
- Technical architecture

---

## Status

| Prototype | Status | Next Action |
|-----------|--------|-------------|
| WooCommerce | ✅ Ready | Test with real shop |
| Stripe | ✅ Ready | Test with test transactions |
| Shopify | ✅ Ready | Test with development store |
| HubSpot Bridge | ✅ Exists | Document workflow |
| sevDesk | ✅ Exists | Expand features |
| DATEV | 🔴 Planned | Research API access |

---

*Generated by Integration Agent - 2024-02-24*
